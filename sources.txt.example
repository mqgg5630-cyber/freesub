# 可选：本地调试时复制为 sources.txt 后填写你有权使用的订阅源，一行一个 URL。
# sources.txt 已被 .gitignore 忽略，避免误提交带密钥的私有订阅。
# GitHub Actions 中请在仓库 Settings → Secrets and variables → Actions → Variables
# 新建变量 SUB_SOURCE_URLS（多个 URL 可换行或用英文逗号分隔）。
# 如果不设置变量，也不创建 sources.txt，脚本会使用 scripts/main.py 里的默认 SOURCE_URLS。

# https://example.com/your-subscription.txt
